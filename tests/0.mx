int main() {
    int la = 5;
    int[] a = new int[la];

    int cnt = 0;
    int i;
    for (i = 0; i < la; ++i) {
        a[i] = ++cnt;
        println(toString(a[i]));
    }

    println("mid");

    int sum = 0;
    for (i = 0; i < la; ++i) {
        sum = sum + a[i];
        println(toString(sum));
    }

    return sum;
}

